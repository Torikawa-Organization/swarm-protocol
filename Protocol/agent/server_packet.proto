syntax = "proto3";
package agent;

import "agent/handshake/server.proto";
import "agent/payload/server.proto";

message ServerPacket {
    oneof inner {
        handshake.ServerHandshakePacket server_handshake_packet = 1;
        payload.ServerPayloadPacket server_payload_packet = 2;
    }
}
